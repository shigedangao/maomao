---
kind: ManagedCertificate
metadata:
  labels:
    name: rusty-certificate
  name: rusty-certificate
version: networking.gke.io/v1
spec:
  domains:
    - rusty-dev.co.kr
    - rusty-dyn.co.kr
---
kind: Workflow
metadata:
  labels:
    generatedName: steps-
version: argoproj.io/v1alpha1
spec:
  entrypoint: hello
  templates:
    - name: hello
      steps:
        - name: hello world
          template: whalesay
          arguments:
            parameters:
              - name: message
                value: hello1
        - name: hello bar
          template: whalesay
          arguments:
            parameters:
              - name: message
                value: hello1
    - name: whalesay
      container:
        args:
          - "{{inputs.parameters.message}}"
        command:
          - cowsay
        image: docker/whalesay
      inputs:
        parameters:
          - name: message
---
kind: "<kind>"
metadata:
  labels:
    foo: bar
version: "<kind_version>"
spec: {}
